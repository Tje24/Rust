<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tueditor.app"
    android:versionCode="1"
    android:versionName="0.1.0">

    <!-- SDK mínimo: Android 5.0 (API 21) para dispositivos 2GB RAM -->
    <uses-sdk
        android:minSdkVersion="21"
        android:targetSdkVersion="33"
        android:maxSdkVersion="35" />

    <!-- Permisos necesarios -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- OpenGL ES 2.0+ (para raylib) -->
    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true" />

    <!-- Vulkan (opcional, para wgpu) -->
    <uses-feature
        android:name="android.hardware.vulkan.level"
        android:required="false" />

    <!-- Soporte de texturas comprimidas -->
    <supports-gl-texture android:name="GL_EXT_texture_compression_etc2" />
    <supports-gl-texture android:name="GL_KHR_texture_compression_astc_ldr" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.NoTitleBar.Fullscreen"
        android:hasCode="false"
        android:extractNativeLibs="true"
        android:largeHeap="true"
        android:usesCleartextTraffic="true">

        <!-- Actividad principal -->
        <activity
            android:name="android.app.NativeActivity"
            android:label="@string/app_name"
            android:configChanges="orientation|keyboardHidden|screenSize|screenLayout|smallestScreenSize"
            android:screenOrientation="userLandscape"
            android:exported="true">

            <meta-data
                android:name="android.app.lib_name"
                android:value="rust_editor" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Proveedor de archivos para compartir escenas -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
